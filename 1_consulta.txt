He realizado las correcciones sugeridas al código del procedimiento del dialogo WndProc y al procedimiento de dialogo DlgProc1, cuando se hace el llamado
al dialogo DlgProc1, se muestra la ventana de dialogo, pero se congela.  He incorporado el procedimiento del cuadro de dialog About, que funciona correctamente y 
no da problemas.  Pero al activar el DlgProc1, se congela la aplicación.


LRESULT CALLBACK WndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam)
{
    static MatrixD datos;
    static MatrixD* dat = &datos;

    switch (message)
    {
    case WM_COMMAND:
    {
        int wmId = LOWORD(wParam);

        switch (wmId)
        {
        case IDM_NUEVO:
            MessageBoxW(hWnd, L"Nuevo Proyecto", L"RESULTADO CORRECTO", MB_ICONEXCLAMATION | MB_OK);
            DialogBoxParamW(hInst, MAKEINTRESOURCE(IDD_DIALOGBAR), hWnd, (DLGPROC)DlgProc1, (LPARAM)dat);
            return 0;   // ✔️ OK

        case IDM_EXISTENTE:
            MessageBoxW(hWnd, L"Proyecto Existente", L"RESULTADO CORRECTO", MB_ICONEXCLAMATION | MB_OK);
            return 0;

        case IDM_EDITAR:
            MessageBoxW(hWnd, L"Proyecto a Editar", L"RESULTADO CORRECTO", MB_ICONEXCLAMATION | MB_OK);
            return 0;

        case IDM_ABOUT:
            DialogBox(hInst, MAKEINTRESOURCE(IDD_ABOUTBOX), hWnd, About);
            return 0;

        case IDM_EXIT:
            DestroyWindow(hWnd);
            return 0;

        default:
            return DefWindowProc(hWnd, message, wParam, lParam);  // ✔️ IMPORTANTE
        }
    }

    case WM_PAINT:
    {
        PAINTSTRUCT ps;
        HDC hdc = BeginPaint(hWnd, &ps);
        EndPaint(hWnd, &ps);
    }
    return 0;

    case WM_DESTROY:
        PostQuitMessage(0);
        return 0;
    }

    return DefWindowProc(hWnd, message, wParam, lParam);  // ✔️ CRÍTICO
}


BOOL CALLBACK DlgProc1(HWND hDlg, UINT msg, WPARAM wParam, LPARAM lParam)
{
    static MatrixD* Datbas;

    switch (msg)
    {
    case WM_INITDIALOG:
        Datbas = (MatrixD*)lParam;
        SetDlgItemTextW(hDlg, IDC_EDIT1, Datbas->NombreProy);
        SetDlgItemInt(hDlg, IDC_EDIT2, Datbas->ng, TRUE);
        SetDlgItemInt(hDlg, IDC_EDIT3, Datbas->nt, TRUE);
        SetDlgItemInt(hDlg, IDC_EDIT4, Datbas->nl, TRUE);
        SetDlgItemInt(hDlg, IDC_EDIT5, Datbas->nm, TRUE);
        return TRUE;

    case WM_COMMAND:
        switch (LOWORD(wParam))
        {
        case IDOK:
        {
            BOOL Bnum = FALSE;
            GetDlgItemTextW(hDlg, IDC_EDIT1, Datbas->NombreProy, 80);
            Datbas->ng = GetDlgItemInt(hDlg, IDC_EDIT2, &Bnum, TRUE);
            Datbas->nt = GetDlgItemInt(hDlg, IDC_EDIT3, &Bnum, TRUE);
            Datbas->nl = GetDlgItemInt(hDlg, IDC_EDIT4, &Bnum, TRUE);
            Datbas->nm = GetDlgItemInt(hDlg, IDC_EDIT5, &Bnum, TRUE);
            EndDialog(hDlg, IDOK);
        }
        return TRUE;

        case IDCANCEL:
            MessageBoxW(hDlg, L"No se cargaron los datos", L"PROCESO TERMINADO", MB_ICONEXCLAMATION | MB_OK);
            Datbas->abortar = 0;
            EndDialog(hDlg, IDCANCEL);
            return TRUE;
        }
        break;
    }

    return FALSE;
}



INT_PTR CALLBACK About(HWND hDlg, UINT message, WPARAM wParam, LPARAM lParam)
{
    UNREFERENCED_PARAMETER(lParam);
    switch (message)
    {
    case WM_INITDIALOG:
        return (INT_PTR)TRUE;

    case WM_COMMAND:
        if (LOWORD(wParam) == IDOK || LOWORD(wParam) == IDCANCEL)
        {
            EndDialog(hDlg, LOWORD(wParam));
            return (INT_PTR)TRUE;
        }
        break;
    }
    return (INT_PTR)FALSE;
}


El siguiente es el codigo del IDD_DIALOGBAR

IDD_DIALOGBAR DIALOGEX 0, 0, 330, 172
STYLE DS_SETFONT | DS_FIXEDSYS | WS_CHILD
FONT 8, "MS Shell Dlg", 400, 0, 0x0
BEGIN
    LTEXT           "Nombre del Proyecto a crear:",IDC_STATIC,8,26,101,11
    EDITTEXT        IDC_EDIT1,110,23,115,14,ES_AUTOHSCROLL
    LTEXT           "DATOS DEL PROYECTO A ANALIZAR Y CREAR",IDC_STATIC,73,6,161,8
    RTEXT           "Número de Generadores:",IDC_STATIC,41,54,102,8
    RTEXT           "Número de Transformadores:",IDC_STATIC,42,70,102,8
    RTEXT           "Número de Líneas:",IDC_STATIC,43,88,100,8
    RTEXT           "Número de Motores:",IDC_STATIC,43,106,100,8
    EDITTEXT        IDC_EDIT2,150,49,21,14,ES_AUTOHSCROLL
    EDITTEXT        IDC_EDIT3,150,67,21,14,ES_AUTOHSCROLL
    EDITTEXT        IDC_EDIT4,150,86,21,14,ES_AUTOHSCROLL
    EDITTEXT        IDC_EDIT5,150,104,21,14,ES_AUTOHSCROLL
    DEFPUSHBUTTON   "Aceptar",IDOK,201,151,50,14
    DEFPUSHBUTTON   "Cancelar",IDCANCEL,266,150,50,14
END
